server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/empmanagement
    username: postgres
    password: farakdadapeer
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
